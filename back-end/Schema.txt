CREATE TABLE Department (
  id int NOT NULL AUTO_INCREMENT,
  name varchar(255) NOT NULL,
  hod varchar(255) NOT NULL,
  contact int,
  PRIMARY KEY (id)
);

CREATE TABLE Student (
  id int NOT NULL AUTO_INCREMENT,
  role int NOT NULL,
  fname varchar(55) NOT NULL,
  lname varchar(55) NOT NULL,
  email varchar(255) NOT NULL,
  password varchar(255) NOT NULL DEFAULT 'mypassword',
  city varchar(55) NOT NULL,
  contact int,
  PRIMARY KEY (id)
);

CREATE TABLE DepartmentStudent (
  id int NOT NULL AUTO_INCREMENT,
  StudId int,
  DeptId int,
  FOREIGN KEY (StudId) REFERENCES Student(id),
  FOREIGN KEY (DeptId) REFERENCES Department(id),
  PRIMARY KEY (id)
);

INSERT INTO Department (name, hod, contact)
value ("IT", "Amol Jain", 9999999999);

INSERT INTO Student (role, fname, lname, email, password, city, contact) value (1, "Sharvari", "Shinde", "sharvari@gmail.com", "sharvari", "Pune", 888888888);


INSERT INTO DepartmentStudent (StudId, DeptId)
value (1, 3);

SELECT
    `Student`.*
FROM
    `Student`
    JOIN `DepartmentStudent` ON `Student`.`id` = `DepartmentStudent`.`StudId`
WHERE
    `DepartmentStudent`.`DeptId` = 1;
